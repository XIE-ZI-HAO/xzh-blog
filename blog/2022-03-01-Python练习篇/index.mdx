---
slug: Python
title: 😜Python练习😜
authors: [XZH]
tags: [Python练习]
---

个人练习 🐱‍🚀

<!-- truncate -->

## 😜Python 练习 😜

```Py

import math //math数据库
r = eval(input("请输入圆的半径："))//eval用来执行一个字符串表达式,并返回表达式的值
print(r)
s1 = math.pi * r * r
print("圆的面积s={0:.4f},圆的半径 r ={0:.1f}".formst(s1,r))//{}:表示槽的意思。.4f:是保留小数点4位。.forst:表示显示出半径和面积

```

```Py

total = eval(input(请输入总数：))
first = total-3*4
num = round (first/2.5)+1#
print ("次数："，num)

```

```Py

print(round(456.123456, 2))#round:表示保留小数点后两位

```

::: tip 重要函数
eval、import math、、round
:::

```py

total = eval(input("请输入总量："))
first = total - 3 * 4
num1 = first / 2.5        #商
num2 = first // 2.5    #商整数部分
if num1 > num2:  #如果num1大于num2,则num2+1
    n = num2 + 1
else:      #判断
    n = num2
    print(num1,num2)

```
